ultracode — Olympic Sprint III, Wave 2 (Connectors & scale — full-market ingestion + startup intake). The Dispatch OS / Cooperative Markets repo folder is connected as context — LIST it and read from it before anything else (do not assume; verify the files exist). Multi-agent orchestration is authorized: fan out agents/workflows per independent workstream; you integrate and run the debug gate. Adversarially verify before committing.
ORIENT FIRST — read these in order, then confirm you're oriented in one short paragraph:
1. README.md
2. docs/00_governance/DOCTRINE.md
3. docs/00_governance/CURRENT_STATE.md
4. docs/00_governance/ACTIVE_BUILD.md
5. docs/00_governance/BUILD_PROGRESS.md   (the % tracker — Sprint III Wave 1 closed at ~54%; keep it current, recompute HONESTLY)
6. docs/00_governance/OLYMPIC_SPRINT_ROADMAP.md   (Sprint III target ~68% — re-baseline vs the real ~54% per caveat #4 before projecting)
7. HANDOFF.md
8. DEBUG_LOG.md
9. docs/00_governance/SPRINT_PLAN.md
Then skim what Wave 2 builds on (the Wave-1 connector stack): core/kernel/connector_sdk.ts (the Connector Output Contract, parser contract, recordToObservation — normalize→truth bridge tiering FROM THE SOURCE MANIFEST), core/kernel/connector_runtime.ts (the generic executor — authorize-first, retry/circuit-breaker, change detection, health, envelope-correlated event/cost; a rejection is NEVER a deletion), core/registry/{connectors.ts,data/connectors.json} (the config-as-data catalog + closed-graph loader — 39 sources/connectors), cartridges/cooperative_markets/connectors/{ncua_5300_connector,ncua_regulations_connector}.ts + run_connectors.ts (two real connectors THROUGH the runtime; 5300 → persisted plane-aware profiles reconciling to source), cartridges/cooperative_markets/{batch_fixtures,ingest_batch,ingest_call_report,deal_engine}.ts (the batch the bulk feed replaces + the deal engine the intake feeds), core/profile/persistence.ts (the persist seam), scripts/debug-loop.mjs (13 steps, incl. CONNECTOR), tests/*.test.mjs (246 unit tests).
STATE (do not regress): Migrations 0001–0017 APPLIED; 0018 written + additive, Bryan-only apply. Sprint II shipped identity+permissions, live profile assembly+query, the kernel envelope+contracts, and the registry live-persistence + matured resolution + profile persistence. Sprint III Wave 1 shipped the generic Connector Runtime + SDK (RFC-2011): connectors NORMALIZE only; tier/plane from the source manifest; a run is authorized+correlated through a kernel envelope emitting KernelEvent+CostEntry; a config-as-data catalog (39 connectors) qualifies the DKR placeholders; two real connectors run end-to-end (5300 → persisted profiles reconciling to source; the REAL 675-section 12 CFR corpus at scale). debug-loop ALL GREEN (13/13), 246 unit tests. Default stays in-memory; Supabase adapters drop in when creds + 0018 are set.
ANTI-CLOBBER (Bryan's rule) — BEFORE changing anything: run `git status` in the repo and confirm the working tree is clean. The last pushed commit should be the Sprint-III-Wave-1 commit. Never overwrite a file on disk without first confirming it hasn't changed since you last read it.
EXECUTE — Sprint III Wave 2 (full-market ingestion + startup intake; target the ~60s toward ~68%): run the network on BREADTH of real sources, additive; config-as-data; no vertical nouns in core/; the connector runtime is UNCHANGED (verticals declare connectors as config + a parser). Suggested shape (fan out; a wave per independent workstream, checkpoint each):
* Full-market NCUA 5300 at scale — swap the 5300 connector's `acquire` to a BULK data source (the runtime is source-agnostic; only the injected data changes), running the WHOLE market → institution profiles PERSISTED at scale through the profile seam, RETIRING batch_fixtures.ts behind the connector. Keep evidence/source/confidence/lineage; ratios stay a downstream deterministic_calculation, NEVER a conclusion in a weight. (Real bulk per-CU 5300 data is Bryan-only — if it hasn't landed, generate a clearly-LABELED at-scale synthetic market behind the same connector so the SCALE path is proven honestly, and note it.) Moves truth 47→~58, connectors 45→~60, cartridge up.
* Startup-intake connector → the deal engine — wire source:startup_intake through the runtime so the deal engine (P1 scoring / P2 IC memo) runs on real intake, not seed inputs (the DEFERRED "live intake path"). Normalized intake → sourced readiness inputs → the existing engine; the human gates (ICApproval + EditorialDisposition) untouched. Moves harness/cartridge up.
* Qualify the remaining catalog placeholders toward the ~93 — add config-as-data ConnectorSpec + SourceRegistryEntry manifests (SEC EDGAR/Form D/ADV, FFIEC, state regulators, vendor/trust/status pages, etc.) with at least one more real connector implemented against the SDK. Config-as-data, not code forks. Moves connectors up.
* Add a debug-loop assertion + unit tests per new connector/runtime path (output-contract shape; change_event/quality_report determinism; a run correlates to its envelope; persisted profiles reconcile to source; a rejection is never a deletion; tier-from-source proven with a differing source).
OPERATING RULES (every wave): additive only; config-as-data; no vertical nouns in core/; NEVER a regulated/financial conclusion in weights (persist evidence/source/confidence/lineage; route regulated conclusions + publication through the human gates — ICApproval + EditorialDisposition); truth tiers/planes not conflated; pure/deterministic modules (caller injects ids/timestamps). Connectors NORMALIZE — no business logic in a connector; tier/plane from the source manifest.
DEBUG AS YOU GO: log to DEBUG_LOG.md tagged [BLOCKER]/[NON-BLOCKING]/[DEFERRED]; fix blockers now.
DEBUG GATE before a wave is done: `npm install` once if needed, then `node scripts/debug-loop.mjs` ALL GREEN (now includes CONNECTOR — ADD a new debug-loop assertion + unit tests for every new engine/service/connector per wave) + `npx tsc --noEmit` clean + `npm run build` exit 0 + `npm test` green. Adversarially verify findings (4-lens: correctness · truth-doctrine/no-core-vertical-leak · purity/determinism · test-teeth) before committing.
CHECKPOINT + CONTEXT DISCIPLINE (Bryan's rules):
- Checkpoint at the END OF THE WAVE: update BUILD_PROGRESS (recompute % HONESTLY — re-baseline vs the roadmap, don't inflate to a target), CURRENT_STATE, ACTIVE_BUILD, DEBUG_LOG; write a fresh HANDOFF.md; append the next kickoff prompt; give Bryan the exact git add/commit/push command.
- MONITOR YOUR OWN CONTEXT WINDOW. At ~80% capacity, STOP, checkpoint cleanly, hand back a fresh first-prompt for the next chat, and do NOT start work you can't finish. A truncated wave is the failure mode.
- The Dispatch sandbox's git cannot commit/push (restricted VM). Write files to disk, then hand Bryan the exact command to run in his Mac terminal. The cloud session moves files onto the Mac via a staging tarball extracted in the repo (mount blocks overwrite/unlink, so the handback command should `tar xzf` the staging tarball in the repo, then `rm -f` it). Start the command with `rm -f .git/index.lock && rm -rf _to_delete` and remove any `_cloudsync_in.tgz` / `_cloudsync_out.tgz` / `_wave*_incoming.tgz` / `_wave*_srcsnapshot.tgz` stragglers. Verify with `git status` first; never assume a push happened. git push + apply 0018 + a real bulk 5300 feed + the investment-vehicle decision + VC/Alloya legal are Bryan-only — route around, don't block.
Go — orient, verify no lost updates, execute Sprint III Wave 2 with checkpoints, pass the debug gate, and hand back a clean commit command + the next kickoff.
