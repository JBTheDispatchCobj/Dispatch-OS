ultracode — Olympic Sprint III, Wave 5 (the JOINT Terminal UI + continue the path: catalog toward ~93 + reconcile more of the FS canon). The Dispatch OS / Cooperative Markets repo folder is connected as context — LIST it and read from it before anything else (do not assume; verify the files exist). The FS / Dispatch-Auric V1 release package is also connected (DISPATCH_AURIC_V1_MASTER_RELEASE_COMPLETE) — it is the REFERENCE/operational canon adopted in ADR-0017; pull FS-10000 (Terminal UI & User Workspaces) as the reference for the UI wave. Multi-agent orchestration authorized. Adversarially verify before committing.
ORIENT FIRST — read these in order, then confirm you're oriented in one short paragraph:
1. README.md
2. docs/00_governance/DOCTRINE.md
3. docs/00_governance/CURRENT_STATE.md
4. docs/00_governance/ACTIVE_BUILD.md
5. docs/00_governance/BUILD_PROGRESS.md   (the % tracker — Sprint III Wave 4 closed at ~59%; keep it current, recompute HONESTLY)
6. docs/00_governance/OLYMPIC_SPRINT_ROADMAP.md   (Sprint III target ~68%; Terminal is a Sprint IV focus but the first real surface lands here)
7. HANDOFF.md
8. DEBUG_LOG.md
9. docs/01_architecture/adr/ADR-0017-FS-V1-CANON-ADOPTION.md   (the canon seam — identity not authority; repo wave order leads)
Then skim: core/registry/canon.ts + core/registry/data/canon_aliases.json (the canon reconciliation seam), core/registry/{service,resolver,candidate_bridge,connectors}.ts, cartridges/cooperative_markets/{run_market_ingest,run_registry_candidates}.ts (the full-market profiles + the propose-only registry queue the UI renders), the existing Terminal surfaces app/{terminal,market,distribution,observability}/page.tsx + components/terminal/*, scripts/debug-loop.mjs (14 steps), tests/*.test.mjs (297 unit tests).
STATE (do not regress): Migrations 0001–0017 APPLIED; 0018 written + additive, Bryan-only apply. Sprint III shipped: the generic Connector Runtime + SDK (W1); full-market 5300 at scale (labeled synthetic) + startup-intake → deal engine + SEC EDGAR + catalog 39→57 (W2); FDIC BankFind + Federal Register connectors + connector candidates → Object Registry (propose-only) + catalog 57→73 (W3); the FS canon reconciliation seam + ADR-0017 (W4). debug-loop ALL GREEN (14/14), 297 unit tests. Default in-memory; Supabase drops in when creds + 0018 set. Real bulk 5300 NOT landed → full-market 5300 stays LABELED synthetic.
THIS WAVE IS COLLABORATIVE (Bryan's ask): the Terminal UI is built JOINTLY. Do NOT unilaterally finalize the visual design — build the surface over real run output, deliver it, and iterate WITH Bryan on look/feel/priority. Ask his preferences (via AskUserQuestion or plain questions) on layout, what matters most on screen, and tone before locking styling.
ANTI-CLOBBER (Bryan's rule) — BEFORE changing anything: `git status` in the repo, confirm clean; the last pushed commit should be the Sprint-III-Wave-4 commit. If a `_wave*_incoming.tgz` staging tarball is present + uncommitted, APPLY it first (extract) before building — never build on a stale HEAD.
EXECUTE — Sprint III Wave 5 (additive; config-as-data; no vertical nouns in core/; connector runtime UNCHANGED; regulated conclusions + publication stay behind the human gates ICApproval + EditorialDisposition):
* THE JOINT TERMINAL SURFACE — a new Next.js Terminal surface (e.g. app/network/page.tsx + components/terminal/*) over (a) the full-market institution profiles from run_market_ingest (LABELED synthetic until the real 5300 lands — the UI MUST show the synthetic label, never present it as real), and (b) the propose-only REVIEW QUEUE: the cross-source entity duplicates from run_registry_candidates AND the canon alias proposals from canon.ts — rendered as a human-review surface (confirm/reject), NEVER an auto-merge. Missing/stale/inferred/proposed must be visibly distinct (doctrine). `npm run build` must prerender it. Move Terminal 40→toward 50 + truth/harness.
* CANON: reconcile MORE FS-5100 registries via the seam (workflow/agent/event/evidence/approval) — add config-as-data aliases (propose or confirm the unambiguous ones); surface the proposals in the review queue. Grow the crosswalk honestly (propose → confirm → sticky).
* CATALOG: continue 73→~93 (config-as-data manifests) + optionally one more real connector (SEC full-text, Federal Reserve data, FFIEC CDR).
* REAL bulk 5300 IF Bryan staged it (check docs/04_sources/ncua/ for real 5300 financials) — swap the synthetic market's injected acquire batch behind the SAME connector; move truth to real figures HONESTLY. Else keep labeled synthetic + say so.
* Add a debug-loop assertion + unit tests per new surface/path (the review queue renders propose-only, never a merge; the synthetic label is shown; canon proposals surface; deterministic).
OPERATING RULES (every wave): additive only; config-as-data; no vertical nouns in core/; NEVER a regulated/financial conclusion in weights; truth tiers/planes not conflated; pure/deterministic modules (caller injects ids/timestamps); connectors NORMALIZE only; synthetic/illustrative data CLEARLY LABELED and never presentable as real; the canon seam reconciles IDENTITY not AUTHORITY (a label match is not a semantic merge).
DEBUG AS YOU GO: log to DEBUG_LOG.md tagged [BLOCKER]/[NON-BLOCKING]/[DEFERRED]; fix blockers now.
DEBUG GATE before the wave is done: `npm install` once if needed, then `node scripts/debug-loop.mjs` ALL GREEN (ADD a new assertion + unit tests for every new surface/service/connector) + `npx tsc --noEmit` clean + `npm run build` exit 0 + `npm test` green. Adversarially verify (4-lens) before committing.
CHECKPOINT + CONTEXT DISCIPLINE: checkpoint at wave end (recompute % HONESTLY; update CURRENT_STATE, ACTIVE_BUILD, BUILD_PROGRESS, DEBUG_LOG; fresh HANDOFF; next kickoff; exact git command). MONITOR YOUR CONTEXT — at ~80%, STOP, checkpoint cleanly, hand back a fresh first-prompt; a truncated wave is the failure mode. The Dispatch sandbox git cannot push — write files, hand Bryan the exact Mac command: a GLOB-FREE (zsh-safe), explicit-filename command that starts `rm -f .git/index.lock && rm -rf _to_delete`, removes any `_cloudsync_in.tgz`/`_cloudsync_out.tgz`/`_repo_snapshot.tgz`/`_freshsnap.tgz`/`_wave*_incoming.tgz` stragglers by EXPLICIT name (no `*` globs — zsh aborts the line on a no-match), `tar xzf` the staging tarball, `rm -f` it, then `git status` → `git add -A` → `git commit` → `git push`. Verify with `git status` first; never assume a push happened. git push + apply 0018 + real bulk 5300 + investment-vehicle decision + VC/Alloya legal are Bryan-only.
Go — orient, verify no lost updates, build the Terminal surface JOINTLY with Bryan, continue the canon/catalog path, pass the debug gate, hand back a clean commit command + the next kickoff.
