# ADR-0002: Model-Agnostic Harness

Status: Accepted

Dispatch will not create a unique trained model per institution. Institutional uniqueness comes from private context, rules, profile state, permissions, and shared market data. Models are replaceable compute providers selected through a complexity and cost router.
