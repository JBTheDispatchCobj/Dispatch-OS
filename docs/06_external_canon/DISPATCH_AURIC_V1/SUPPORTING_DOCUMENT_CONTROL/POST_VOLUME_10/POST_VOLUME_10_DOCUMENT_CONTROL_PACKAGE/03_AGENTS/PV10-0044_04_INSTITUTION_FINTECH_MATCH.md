# 04 Institution Fintech Match

Institution problem/priority → capability requirement → candidate retrieval → eligibility filters → fit score → readiness gates → relationship path → evidence package → recommendation → workspace or monitor.

## Required implementation fields

Owner, trigger, inputs, objects, relationships, policies, agents, human approvals, outputs, metrics, failure modes, recovery, events, and acceptance tests.
