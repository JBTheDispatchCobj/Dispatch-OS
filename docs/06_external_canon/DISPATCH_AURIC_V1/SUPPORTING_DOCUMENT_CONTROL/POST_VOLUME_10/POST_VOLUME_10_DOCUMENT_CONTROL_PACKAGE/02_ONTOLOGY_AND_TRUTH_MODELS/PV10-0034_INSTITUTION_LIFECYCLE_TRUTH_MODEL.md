# Institution Lifecycle Truth Model

Observed → Identity resolved → Public profile assembled → Verified → Scored → Opportunity detected → Institution engages → Private configuration created → Connectors/data approved → Workspaces activated → Execution measured → Profile and graph updated.

For each transition store trigger, required evidence, authority, source freshness, failure conditions, rollback, events, and outputs.
