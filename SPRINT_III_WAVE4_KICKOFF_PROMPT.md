ultracode — Olympic Sprint III, Wave 4 (Connectors & scale — real bulk 5300 IF landed / finish the catalog toward ~93 + a Terminal surface over the network). The Dispatch OS / Cooperative Markets repo folder is connected as context — LIST it and read from it before anything else (do not assume; verify the files exist). Multi-agent orchestration is authorized: fan out agents/workflows per independent workstream; you integrate and run the debug gate. Adversarially verify before committing.
ORIENT FIRST — read these in order, then confirm you're oriented in one short paragraph:
1. README.md
2. docs/00_governance/DOCTRINE.md
3. docs/00_governance/CURRENT_STATE.md
4. docs/00_governance/ACTIVE_BUILD.md
5. docs/00_governance/BUILD_PROGRESS.md   (the % tracker — Sprint III Wave 3 closed at ~58%; keep it current, recompute HONESTLY)
6. docs/00_governance/OLYMPIC_SPRINT_ROADMAP.md   (Sprint III target ~68% — re-baseline vs the real ~58% per caveat #4 before projecting)
7. HANDOFF.md
8. DEBUG_LOG.md
9. docs/00_governance/SPRINT_PLAN.md
Then skim what Wave 4 builds on (the Wave-1/2/3 connector stack): core/kernel/connector_sdk.ts + connector_runtime.ts (the UNCHANGED generic runtime — authorize-first, retry/circuit-breaker, change detection, health, envelope-correlated event/cost; a rejection is NEVER a deletion — a previously-seen record that fails validation is recovered via the connector's withRef change-key tag, never fabricated as a deletion; tier/plane FROM THE SOURCE MANIFEST), core/registry/{connectors.ts,data/connectors.json} (config-as-data catalog + closed-graph loader — now 73 sources/connectors), core/registry/candidate_bridge.ts (the GENERIC EntityCandidate→CanonicalObjectInput seam), cartridges/cooperative_markets/connectors/{ncua_5300_connector,ncua_regulations_connector,startup_intake_connector,sec_edgar_connector,fdic_bankfind_connector,federal_register_connector}.ts (SIX real connectors THROUGH the runtime), cartridges/cooperative_markets/{bulk_5300_market,run_market_ingest,run_intake,run_sec_edgar,run_fdic_bankfind,run_federal_register,run_registry_candidates,deal_engine,ic_memo}.ts, core/profile/persistence.ts + core/registry/{service.ts,resolver.ts}, scripts/debug-loop.mjs (13 steps; CONNECTOR covers scale + intake + SEC EDGAR + FDIC + Federal Register + connector→registry propose-only + catalog-73), tests/*.test.mjs (290 unit tests).
STATE (do not regress): Migrations 0001–0017 APPLIED; 0018 written + additive, Bryan-only apply. Sprint III Wave 1 shipped the generic Connector Runtime + SDK (RFC-2011). Wave 2 shipped full-market 5300 at scale (labeled synthetic) + startup-intake → deal engine + SEC EDGAR + catalog 39→57. Wave 3 shipped two more real connectors (FDIC BankFind, Federal Register), the connector-candidates → Object Registry propose-only wiring (cross-source dedup PROPOSED, never merged), and catalog 57→73. debug-loop ALL GREEN (13/13), 290 unit tests. Default stays in-memory; Supabase adapters drop in when creds + 0018 are set.
ANTI-CLOBBER (Bryan's rule) — BEFORE changing anything: run `git status` in the repo and confirm the working tree is clean. The last pushed commit should be the Sprint-III-Wave-3 commit (which folded in Wave 2). Never overwrite a file on disk without first confirming it hasn't changed since you last read it. If a `_wave*_incoming.tgz` staging tarball is present and uncommitted, APPLY it first (extract) before building — do not build on a stale HEAD and silently lose the staged wave.
EXECUTE — Sprint III Wave 4 (push connectors + truth + terminal toward the Sprint-III-end numbers; additive; config-as-data; no vertical nouns in core/; the connector runtime is UNCHANGED). Suggested shape (fan out; a wave per independent workstream, checkpoint each):
* REAL bulk 5300 IF it has landed — if Bryan has staged a real per-CU 5300 bulk feed (look under docs/04_sources/ncua/ for real 5300 financials, NOT the regulatory corpus), swap the synthetic market's injected `acquire` batch for it behind the SAME connector (NO code change beyond the data source); rerun the full-market persist/reconcile at real scale; move truth toward 60 HONESTLY (real figures now, not labeled synthetic). If it has NOT landed, keep the labeled synthetic market and SAY SO — do not fabricate realness.
* Finish qualifying the catalog toward the ~93 (73 today) — add the remaining ~20 config-as-data ConnectorSpec + SourceRegistryEntry manifests, and implement one or two MORE real connectors against the SDK (SEC full-text search, Federal Reserve data, FFIEC CDR are prime real next ones; each normalizes real public metadata → tiered from its source manifest). Config-as-data, not code forks.
* A TERMINAL SURFACE over the network — render a Next.js Terminal surface over the full-market institution profiles (from run_market_ingest) and/or the connector→registry candidate queue (the propose-only cross-source duplicates awaiting human review). Moves Terminal 40→toward 50 + harness/truth. Wire it to real run output; `npm run build` must prerender it.
* Add a debug-loop assertion + unit tests per new connector/runtime/surface path (output-contract shape; change_event/quality_report determinism; a run correlates to its envelope; persisted profiles reconcile to source; a rejection is never a deletion — include a PREVIOUSLY-SEEN-valid-key rejection case; tier-from-source proven with a differing source; the registry queue renders propose-only, never a merge).
OPERATING RULES (every wave): additive only; config-as-data; no vertical nouns in core/; NEVER a regulated/financial conclusion in weights (persist evidence/source/confidence/lineage; route regulated conclusions + publication through the human gates — ICApproval + EditorialDisposition); truth tiers/planes not conflated; pure/deterministic modules (caller injects ids/timestamps). Connectors NORMALIZE — no business logic in a connector; tier/plane from the source manifest. Synthetic/illustrative data MUST be clearly LABELED and never presentable as real.
DEBUG AS YOU GO: log to DEBUG_LOG.md tagged [BLOCKER]/[NON-BLOCKING]/[DEFERRED]; fix blockers now.
DEBUG GATE before a wave is done: `npm install` once if needed, then `node scripts/debug-loop.mjs` ALL GREEN (ADD a new debug-loop assertion + unit tests for every new engine/service/connector/surface per wave) + `npx tsc --noEmit` clean + `npm run build` exit 0 + `npm test` green. Adversarially verify findings (4-lens: correctness · truth-doctrine/no-core-vertical-leak · purity/determinism · test-teeth) before committing.
CHECKPOINT + CONTEXT DISCIPLINE (Bryan's rules):
- Checkpoint at the END OF THE WAVE: update BUILD_PROGRESS (recompute % HONESTLY — re-baseline vs the roadmap, don't inflate to a target), CURRENT_STATE, ACTIVE_BUILD, DEBUG_LOG; write a fresh HANDOFF.md; append the next kickoff prompt; give Bryan the exact git add/commit/push command.
- MONITOR YOUR OWN CONTEXT WINDOW. At ~80% capacity, STOP, checkpoint cleanly, hand back a fresh first-prompt for the next chat, and do NOT start work you can't finish. A truncated wave is the failure mode.
- The Dispatch sandbox's git cannot commit/push (restricted VM). Write files to disk, then hand Bryan the exact command to run in his Mac terminal. The cloud session moves files onto the Mac via a staging tarball extracted in the repo (mount blocks overwrite/unlink, so the handback command should `tar xzf` the staging tarball in the repo, then `rm -f` it). Start the command with `rm -f .git/index.lock && rm -rf _to_delete` and remove any `_cloudsync_in.tgz` / `_cloudsync_out.tgz` / `_wave*_incoming.tgz` / `_wave*_srcsnapshot.tgz` / `_repo_snapshot.tgz` / `_freshsnap.tgz` stragglers. Verify with `git status` first; never assume a push happened. git push + apply 0018 + a real bulk 5300 feed + the investment-vehicle decision + VC/Alloya legal are Bryan-only — route around, don't block.
Go — orient, verify no lost updates, execute Sprint III Wave 4 with checkpoints, pass the debug gate, and hand back a clean commit command + the next kickoff.
